import SectionTitle from "../../../SectionTitle/SectionTitle";

const AddItem = () => {
    return (
        <div>
            <SectionTitle></SectionTitle>
        </div>
    );
};

export default AddItem;